Develop the script on the commandline

Send output to STDOUT, check

tweak if needed

play around to make sure that the chain of commands is doing what you want that do

execute the command

fc, and capture
